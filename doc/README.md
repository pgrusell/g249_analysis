# Documentation

In this directory we aim at store all the information concerning how the code is used to perform de analysis of the data. Moreover, information concerning the files, setup and other info is (supposed to) be store here (WIP).

Here there is a list of files/directories:

- **Report:** Detailed analysis/manual of usages of this repository (very very preliminary). It is written in LaTeX and automatically compiled via GitHub workflow when a change in the .tex file is pushed/PRed to the dev branch.
- **files.md:** Table with the conditions under which files were produced (deprecated ?).
- **setupFiles:** This directory contains ATIMA files of the setup.